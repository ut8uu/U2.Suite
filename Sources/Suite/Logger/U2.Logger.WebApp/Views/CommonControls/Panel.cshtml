@model PanelControl

<div id="@Model.Name" class="panel border1px black @Model.Class">
	@{
		foreach (var control in Model.Children)
		{
			await Html.RenderPartialAsync(control.ViewName, control);
		}
	}
</div>
